---
- name: Daily Operations Playbook
  hosts: all
  remote_user: ubuntu

  tasks: 
  - name: Running Ping Check aganist Google's DNS
    shell: ping 8.8.8.8 -c 5 
  - name: Updating Packages 
    shell: sudo apt update -y && sudo apt upgrade -y
